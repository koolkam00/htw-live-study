# Full-data refresh: September 11, 2026, 11:07 AM ET

The ten interactive analyses, all 33 extension packs and weather evidence were rebuilt from [private-export-20260911-1107](https://github.com/koolkam00/htw-live-study/releases/tag/private-export-20260911-1107). Both analysis pins select this release. The original core `live.json` and S/R/RN/P packs retain their existing historical vintage; their producer calculation is not part of this consumer repository.

## Verified source

The freeze is `2026-09-11T15:07:22Z`; the largest recorded ingestion timestamp is `2026-09-11T15:04:44Z`. Downloads are ordinary unencrypted, publicly available assets. Raw files and the database snapshot were inspected outside the website checkout; the ingestion database was not modified.

| Check | Measured result |
| --- | --- |
| Raw and feature records | 4,207,456 each; 34 cities; 240 city-years |
| CORE/FULL | Expected 8/9 archive members; all 8 shared members byte-identical |
| Canonical IDs | Unique, non-null, identical sets; zero missing or orphan records |
| Raw/feature values | Race, city, year, age and name agree, including 9 null names |
| Checkpoint timings | Zero discrepancies above 1 ms across all 9 comparable checkpoints |
| Change from 0336 | 228,796 added; 0 deleted; 0 changed across all raw columns |
| SQLite snapshot | SHA-256 verified; read-only `PRAGMA integrity_check` returned `ok` |
| Producer valid-splits flag | 3,326,908; 1,022,545 sustained-slowdown records among these |
| Missing exact age | 2,758,408 records |
| Weather | 235 unique city-years; 234 pass weather measurement checks |
| Route validity | All 32 profiles still lack historical validity years |

The [release-contract audit](evidence/2026-09-11/refresh-1107/release-contract.json) contains archive/member checksums, timing comparisons and the prior-release delta. [Schema coverage](evidence/2026-09-11/refresh-1107/schema-coverage.json), [expanded checks](evidence/2026-09-11/refresh-1107/expanded-audit.json) and [snapshot integrity](evidence/2026-09-11/refresh-1107/snapshot-audit.json) provide supporting evidence.

September 10's refresh failure is historical. This upload fixes the incompatible archive/schema contract and was successfully rebuilt locally. Its release-triggered [Actions run 34615896877](https://github.com/koolkam00/htw-live-study/actions/runs/34615896877) also succeeded, but used older calculation code without this refresh's edition-quality policy. Its artifacts were therefore not imported.

## Recalculated population

The current parser rejects 616,424 missing/unparsed records, 217,295 non-increasing records and 4,677 outside timing bounds. There are no exact duplicates. This leaves 3,369,060 timing-eligible finishes before source-quality exclusions.

The [release-specific quality policy](../analysis/source_quality.py) removes another **40,901** timing-eligible finishes. It honors invalid split-grid declarations for Honolulu 2016, Rotterdam 2012, Eindhoven 2009/2010 and Frankfurt 2019; excludes partial New York 2008 and Chicago 2016; excludes Tokyo 2026's selected top-finisher PDFs; and honors unresolved Amsterdam 2025 and Valencia 2017 holds. Most invalid grids already fail the timing parser. Amsterdam's conservative hold does not imply that three missing records alone cause a large bias. Counts and reasons are recorded in the [independent source-quality audit](evidence/2026-09-11/refresh-1107/source-quality-audit.json) and every pack's metadata.

The main analysis population is **3,328,159 finishes across 28 eligible cities**. All-runner results include 36,151 records with unrecorded sex; sex-specific views use the recorded category. Feature `valid_splits` incorporates different checks and is not substituted for consumer eligibility. The personalized guide has **1,170,588 exact ages** and **524,323 recent prior-performance benchmarks**, with a 90–720-minute target range. Age-specific findings remain limited to recorded ages, not the whole field.

For this audited release, history joins use `features.record_id = race_records.id`, with finish and all segment timings checked. Supplied runner identities still undergo gender, birth-year and repeated-edition consistency checks. There are 3,008,457 linked eligible finishes, 546,847 consecutive cross-year pairs and zero ambiguous cross-export matches. Benchmarks remain strictly earlier; no same-year result is silently treated as a prior race. Missing feature race dates remain missing, and city-year weather dates are a documented chronology proxy.

## Weather decisions

All three candidates were rerun with the unchanged model, practical threshold, seed and 6,000 course-level bootstrap draws. The cohort has **3,324,934 finishes, 177 editions and 27 cities**. These are observational associations with late-race slowing, not causal pace prescriptions.

| Candidate | Adjusted difference, percentage points | 98.33% interval | Publication |
| --- | ---: | --- | --- |
| Humidity / dew point | +0.64 | −0.52 to +1.59 | Withheld: inconclusive |
| First-four-hours warming | +2.07 | +1.53 to +2.75 | Ready: stable association |
| Start wind speed | +0.24 | −0.36 to +0.78 | Ready: precise small difference over the observed typical contrast |

See [weather methods and exposure contrasts](WEATHER_ANALYSES.md). Independent regression reproduced estimates and bootstrap intervals within `1e-8`. A small wind association for this measure does not rule out headwind, gust or finish-time effects.

## Remaining producer work

1. Finish New York 2008 and Chicago 2016; reconcile the Amsterdam 2025 and Valencia 2017 holds. Publish edition-level status, expected/retrieved counts and split-grid validity in a machine-readable release asset so consumer exclusions do not rely on narrative audits. Preserve explicit selected-field labels for Tokyo 2026.
2. Correct or replace the force-dropped grids, keeping invalid rows marked until a documented re-audit clears them. Preserve canonical IDs and publish additions/deletions/changes against this frozen release.
3. Repair Helsinki 2025's scheduled-start/weather-hour mismatch; populate supported race dates and historical route-validity years. Expand exact ages only when the source supplies them; never estimate ages from category labels.
4. Fix manifest self-size (reported 2,286 bytes, actual 4,862) and stale packaging/privacy/missing-feature prose. Keep archive-member hashes, SQLite integrity evidence, public stable links and synchronized CORE/FULL exports.

These are source-quality and metadata follow-ups, not blockers to publishing the screened population above. No additional data credentials are missing for this release.

## Reproduction and validation

Use the commands in [Operations](OPERATIONS.md) with `private-export-20260911-1107`. `analysis/audit_release.py` verifies the current raw/feature contract and optionally CORE alignment and the prior-release delta. The analysis workflows rebuild from the pinned public FULL archive and run the calculation tests; imports independently validate allowed files, cohorts, hashes and source-quality policy.

Local validation: all 27 Python tests passed; all 33 packs (47 charts, 1,901 chart rows) and the 60-file personalized artifact passed import checks. Independent checks reconciled population, age, history and weather results. `npm run verify:data` passed across all 29 course selections (including All courses): 1,160 profiles and 8,946 charts. `npm run build` passed and generated 139 pages. Browser checks verified current source labels, the new Vienna comparison, miles/metric conversion, sparse Melbourne weather selection and both supported weather findings with no browser errors. Production publication is recorded separately after the pull request checks complete.


### GitHub reproduction and provenance repair

The [pacing workflow](https://github.com/koolkam00/htw-live-study/actions/runs/34621938489) and [weather workflow](https://github.com/koolkam00/htw-live-study/actions/runs/34621938454) both completed successfully on `a1c57128aaeecc1f9e1713410387e7555f4714be`. All four downloaded artifact digests matched GitHub. The current 33 packs and personalized guide were imported from that Linux calculation. Weather's numerical results match the independently reviewed local output.

The [artifact comparison](evidence/2026-09-11/refresh-1107/ci-comparison.json) checked 57,054,069 numeric values after matching rows by cohort keys. Aggregate tables and weather matched within floating-point tolerance. Seven personalized gain averages differed by one fourth-decimal unit (0.0001 minutes, or 0.006 seconds), consistent with rounding at a floating-point tie; the GitHub values are authoritative.

Final review caught two empty-file hashes in the local publication metadata, caused by source files being offloaded during calculation: `ext_earlier_best_section_gains.analysis_script_sha256` and `ext_near_miss_recorded_return.supporting_script_sha256`. Fresh CI outputs replace those artifacts. The importers now require exact current-release builder hashes, reject empty files and mismatched code, and preserve historical-code semantics for older releases. Regression tests reproduce this failure. The new [website data validation workflow](../.github/workflows/site-validation.yml) runs all published-data, source-provenance and display checks on PRs and main.
